version: '3'

services:
  database:
    image: database:v1
    ports:
      - "3306:3306"
    links:
      - app 
    networks:
      - app-network
      
  app:
    image: tienda:v1
    ports:
      - "8000:80"
    network:
      - app-network

networks:
    app-network:
      driver: bridge